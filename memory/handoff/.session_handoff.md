# Session Handoff — 2026-06-08 14:41 → 22:30（~8h）

record_protocol:
  skill_loaded: huihua-handoff
  skill_version: 2.0.0
  record_type: session_handoff
  blocker_status: none
  artifact_quality: pass
  closeout_eligible: false

## 重大突破

今晚完成了首次完整 6 模型平行世界推演，全部跑通。核心发现：
- 同一事件，3 个模型判高风险、3 个判中风险
- ds 是唯一发现 `transparency_risk` 的模型（其他 5 个都没看到）
- 不同模型建构出完全不同的舆论场（反对群体从 0 到 3 个）
- 产品核心价值叙事确立：**"不是预测未来，而是暴露盲区"**

详见 `docs/product/parallel_worlds_report_v0.1.md`

## Active Tasks

### 主线：平行世界调度器 bypass 验证 + 产品化

✅ 配置 UI（前端 + 后端） — `tools/probe_scheduler/config_ui.html` + `config_ui.py`
✅ 实时监控页（tab切换 + run.log + 底部总控）
✅ 输出路径 OCP 重构 — `src/output_paths.py`（DefaultRunPaths + ParallelRunPaths）
✅ Repair Agent — `src/phase1/repair_agent.py`（Compiler → Repair → Agent 三级降级）
✅ Compiler 修 typical_phrases 截断
✅ utils.py 修中文逗号
✅ simulation_dataset 补 relations 字段
✅ 竞品调研报告 — `docs/competitive_gaps.md`
✅ 产品推演报告 — `docs/product/parallel_worlds_report_v0.1.md`
✅ 6 模型完整推演（全部通过）
✅ 模型选型建议
🔄 待做：
- `_kill_stale_executors()` 和 `tmux-session-gate.py` 的 Hermes 保护已修（别杀自己）
- `scheduler.py` 的 `_find_run_dir_for_world` 兜底需要修（parallel 模式不应 fallback 到旧目录扫描）
- phases 切换后前端监控页需要重启服务
- `analyzer.py` 的干扰检测可以跑 `parallel_211138` 的数据

## Key Findings

1. **ds 的 outlier 价值已确认** — 唯一发现透明度风险的模型，产品差异化的核心
2. **minimax 通过 Repair Agent 保住了** — 第一次跑成功（之前两轮都挂了）
3. **122b-sg 太慢且疑似 herd bias** — 1800s，0 反对群体，建议淘汰
4. **TKE 节点确实快** — 80b-tke 256s vs 30b-tke 287s，大模型反而不慢
5. **模型选型建议：** 36-35b(基线) + 30b-tke(压力) + ds(深度) + minimax(多样性) + v4f(快速对照)

## Key Files

- 平行调度器 UI: `tools/probe_scheduler/config_ui.py` + `config_ui.html`
- 输出路径策略: `src/output_paths.py`
- Repair Agent: `src/phase1/repair_agent.py`
- 竞品差距报告: `docs/competitive_gaps.md`
- 产品推演报告: `docs/product/parallel_worlds_report_v0.1.md`
- 本次推演数据: `outputs/runs/2026-06-08/parallel_211138/`
- tmux session gate: `~/.hermes/scripts/tmux-session-gate.py`
- kill_stale_executors: `WorkflowBase/runner/cli.py`

## 重要教训

- 模型切换（MiMo → DeepSeek）导致上一个会话被强制终止，handoff 彻底过时。文件系统是唯一可靠来源。
- `_kill_stale_executors` 会误杀 Hermes 进程（已修：加 hermes 过滤）
- `tmux-session-gate.py` 的 `--apply` 模式会杀所有非 workyb/adarian 前缀的 tmux session（已修：加 hermes 白名单）
- osascript 路径空格要用单引号包裹，不能用双引号（AppleScript 会吃双引号）

## next_action: 明天跑一轮包含 deepseek-v4f 的平行推演，验证 DeepSeek 族能否稳定复现透明度风险
